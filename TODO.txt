# TODO

- partition capacity to Float format (to allow for ratio of 1.1, 1.2, etc.)

- interconnection weight representing 'delay'
  (longer delay = higher weight = less desirable, for farther away nodes)
  as in SCOTCH (SCOTCH takes only the lower triangular part of the matrix)